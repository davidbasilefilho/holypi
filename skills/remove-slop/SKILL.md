---
name: remove-slop
description: Removes low-signal generated-code artifacts without changing intended behavior. Use for generated verbosity and needless abstraction, not general feature work.
---

# Remove slop

Remove redundant comments, needless wrappers, defensive branches without a real invariant, style drift, vague names, duplicated prose, and gratuitous abstraction. Preserve useful rationale, compatibility handling, and project conventions. Run formatting, types, and focused tests.
