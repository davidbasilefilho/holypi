---
name: lsp-setup
description: Fix missing, unhealthy, or wrongly rooted LSP support; use lsp when healthy.
---

# LSP setup

Identify languages, package manager, root, config, available servers. Prefer repo-local dependencies/documented commands; avoid global installs unless requested. Validate startup, root, index, definition lookup, diagnostics.
