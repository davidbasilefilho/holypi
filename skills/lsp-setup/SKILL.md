---
name: lsp-setup
description: Diagnoses and configures language-server support for a repository.
---
# LSP setup

Identify languages, package manager, workspace root, existing configuration, and available servers. Prefer repository-local dependencies and documented commands. Avoid global installation unless requested. Validate startup, root detection, indexing, definition lookup, and diagnostics.
