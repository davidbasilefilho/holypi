---
name: ast-grep
description: Syntax-aware search/rewrite when structure matters; use text search for literals.
---

# ast-grep

Confirm `ast-grep`/`sg` and language. Preview a syntax-constrained pattern, inspect matches, then rewrite narrowly. Review diff; run format, types, focused tests. Use grep for literals.
