---
description: Explore, fix decisions, implement, and verify a change
argument-hint: "<task>"
---
Deliver this end to end: $@. First use `holy_workflow` for independent explorer/librarian evidence as needed. Fix architecture, behavior, ownership, and proof in the parent. Then delegate isolated writes to worker only when file ownership does not overlap. Integrate and run final verification yourself.
