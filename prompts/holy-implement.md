---
description: Explore, fix decisions, implement, and verify a change
argument-hint: "<task>"
---

Deliver end to end: $@. Use `holy_workflow` adaptively: gather only material explorer or librarian evidence, fix architecture and behavior in the parent, then run workers only with settled acceptance criteria and nonoverlapping file ownership. Use later explorer or worker calls for independent checks when useful. Inspect failures and diffs, integrate all writes, and run final verification in the parent before reporting completion.
